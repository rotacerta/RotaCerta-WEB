@model IEnumerable<PBP_Frontend.Models.ChangeLog>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_InternalLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.List.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ListStatus.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.List.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ListStatus.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ChangeLogId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ChangeLogId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ChangeLogId })
        </td>
    </tr>
}

</table>

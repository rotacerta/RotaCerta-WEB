@model IEnumerable<PBP_Frontend.Models.ListStatus>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_InternalLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ListStatusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ListStatusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ListStatusId })
        </td>
    </tr>
}

</table>
